0a. Study `specs/*` with up to 250 parallel Sonnet subagents to learn the application specifications.
0b. Study @IMPLEMENTATION_PLAN.md (if present) to understand the plan so far.
0c. Study `anush-portfolio-next/src/lib/*` with up to 250 parallel Sonnet subagents to understand shared utilities & components. If that directory is missing or empty, still study `anush-portfolio-next/src/*` for shared patterns.
0d. For reference, the application source code is in `anush-portfolio-next/src/*`.

1. Study @IMPLEMENTATION_PLAN.md (if present; it may be incorrect) and use up to 500 Sonnet subagents to study existing source code in `anush-portfolio-next/src/*` and compare it against `specs/*`. Use an Opus subagent to analyze findings, prioritize tasks, and create/update @IMPLEMENTATION_PLAN.md as a bullet point list sorted in priority of items yet to be implemented. Ultrathink. Consider searching for TODO, minimal implementations, placeholders, skipped/flaky tests, and inconsistent patterns. Study @IMPLEMENTATION_PLAN.md to determine starting point for research and keep it up to date with items considered complete/incomplete using subagents.

IMPORTANT: Plan only. Do NOT implement anything. Do NOT assume functionality is missing; confirm with code search first. Treat `anush-portfolio-next/src/lib` (when used) as the project's standard library for shared utilities and components. Prefer consolidated, idiomatic implementations there over ad-hoc copies.

ULTIMATE GOAL: Ship a **personal portfolio** (Next.js in `anush-portfolio-next/`) whose **visitor-facing surface** matches `specs/personal-portfolio.md` and `specs/minimal-surface.md`: server-first, semantic HTML, **minimal CSS** (document vibe, not app chrome). Anush’s work, story, and contact; strong a11y and performance, maintainable structure—aligned with `specs/*`. Consider missing elements and plan accordingly. If an element is missing, search first to confirm it doesn't exist, then if needed author the specification at specs/FILENAME.md. If you create a new element then document the plan to implement it in @IMPLEMENTATION_PLAN.md using a subagent.
