# Implementation plan

Disposable prioritized task list. Regenerate in **plan** mode if this drifts from reality.

## Now

- [x] **Minimal surface + same home copy** — `layout.tsx` / `globals.css` / `page.tsx`: structural + CSS refactor per `specs/minimal-surface.md`; **preserve existing default Next.js starter text, links, and images** on the home page.

## Next

- [ ] **About, projects, contact** — real copy and a simple projects data source; contact path per `specs/personal-portfolio.md`.
- [ ] **A11y pass** on expanded pages: heading order, focus, alt text.
- [ ] **Tests** (optional): add a runner + smoke; document `npm test` in `AGENTS.md`.

## Done / icebox

- Minimal global CSS (no Tailwind on page surface) + semantic `<main>` home — see git history when completed.
